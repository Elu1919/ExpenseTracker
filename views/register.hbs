<div class="row mt-5">
  <div class="col-md-4 m-auto">
    <div class="card card-body">
      <h1 class="text-center mt-3">帳號註冊</h1>

      {{> message}}

      <form action="/users/register"
            method="POST">
        <div class="mb-3">
          <label for="name"
                 class="mb-2">名稱*</label>
          <input type="text"
                 id="name"
                 name="name"
                 value="{{ name }}"
                 class="form-control"
                 placeholder="Enter Name">
        </div>
        <div class="mb-3">
          <label for="email"
                 class="mb-2">電子郵件*</label>
          <input type="email"
                 id="email"
                 name="email"
                 value="{{ email }}"
                 class="form-control"
                 placeholder="Enter Email">
        </div>
        <div class="mb-3">
          <label for="password"
                 class="mb-2">密碼*</label>
          <input type="password"
                 id="password"
                 name="password"
                 value="{{ password }}"
                 class="form-control"
                 placeholder="Enter Password">
        </div>
        <div class="mb-3">
          <label for="confirmPassword"
                 class="mb-2">請再輸入一次密碼*</label>
          <input type="password"
                 id="confirmPassword"
                 name="confirmPassword"
                 value="{{ confirmPassword }}"
                 class="form-control"
                 placeholder="Confirm Password">
        </div>
        <div class="d-grid gap-2 mt-5">
          <button type="submit"
                  class="btn btn-primary btn-block border-0"
                  style="background:#e95420">註冊</button>
        </div>
      </form>
      <p class="lead mt-4 fs-6 fw-bold d-flex justify-content-center">
        已經有帳號了嗎?　 <a href="/users/login">點這邊登入帳號</a>
      </p>
    </div>
  </div>
</div>