<div class="row mt-5">
  <div class="col-md-4 m-auto">
    <div class="card card-body">
      <h1 class="text-center mt-3">帳號登入</h1>

      {{> message}}

      <form action="/users/login"
            method="POST">
        <div class="mb-3">
          <label for="email"
                 class="mb-2">Email</label>
          <input type="email"
                 id="email"
                 name="email"
                 class="form-control"
                 placeholder="Enter Email">
        </div>
        <div class="mb-3">
          <label for="password"
                 class="mb-2">Password</label>
          <input type="password"
                 id="password"
                 name="password"
                 class="form-control"
                 placeholder="Enter Password">
        </div>
        <div class="d-grid gap-2 mt-5">
          <button type="submit"
                  class="btn btn-primary btn-block border-0"
                  style="background:#e95420">Login</button>
          <a href="/auth/facebook"
             class="btn btn-outline-primary d-block"><i class="fa-brands fa-facebook"
               style="color: #034cc9;"></i> Facebook Login</a>
        </div>
      </form>
      <p class="lead mt-4 fs-6 fw-bold d-flex justify-content-center">
        還沒有帳號嗎?　 <a href="/users/register"> 點這邊註冊</a>
      </p>
    </div>
  </div>
</div>